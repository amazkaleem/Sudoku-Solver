// Utils/Constants.h
#pragma once

// Global constants (NO namespace)
const int BOARD_SIZE = 9;
const int EMPTY_CELL = 0;
const int BOX_SIZE = 3;
const int CELL_COUNT = BOARD_SIZE * BOARD_SIZE;

// Difficulty enum
enum Difficulty { EASY = 0, MEDIUM = 1, HARD = 2 };